# OpenTroop Frontend

Reserved for the Next.js + Tailwind CSS + shadcn/ui client. To be scaffolded in
a later session. Offline support for field use (camps without cellular service)
will be delivered via the native iOS / Android applications with local caching
and background sync against the backend's sync-aware schema.
